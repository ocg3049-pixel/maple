@Component
script loginbackground extends Component

	@ExecSpace("ClientOnly")
	method void OnBeginPlay()
		local accountText = _EntityService:GetEntityByPath("/ui/LoginScene/Stage/LoginGroup/LoginBackground/AccountText")
		if not isvalid(accountText) then return end
		local player = _UserService.LocalPlayer
		if player == nil then return end
		accountText.TextGUIRendererComponent.Text = player.Name
	end

end

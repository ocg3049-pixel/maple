@Component
script createcharacter extends Component

	@ExecSpace("ClientOnly")
	@EventSender("Self")
	handler HandleButtonClickEvent(ButtonClickEvent event)
		-- 기존 버튼도 새 로그인 단계와 동일한 검사를 거친다.
		_LoginFlow:Action("create")
	end

end

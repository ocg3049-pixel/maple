@Component
script LoginActionButton extends Component
    property string Action = ""

    @ExecSpace("ClientOnly")
    @EventSender("Self")
    handler HandleClick(ButtonClickEvent event)
        -- 화면 상태와 중복 클릭 처리는 세션 컨트롤러 한 곳에서 관리한다.
        _LoginFlow:Action(self.Action)
    end
end
